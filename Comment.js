class Comment {
  constructor(content) {
    this.content = content;
  }

  saveToStorage() {
    // this function was called for in the spec but we didnt use it
  }

  deleteFromStorage() {
    // this function was called for in the spec but we didnt use it
  }
}
