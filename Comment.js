class Comment {
  constructor(content) {
    this.content = content;
  }

  saveToStorage() {
// (should only have one job which is to save the instance to storage)
  }

  deleteFromStorage() {

  }
}
